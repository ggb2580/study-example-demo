<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.clouddemo.paymentdemo.mapper.PaymentMapper">

    <!--添加支付信息-->
    <insert id="addPayment" parameterType="payment" useGeneratedKeys="true" keyProperty="id">
        insert into payment(serialNo,money) values(#{serialNo},#{money})
    </insert>

    <!--根据id查询支付信息-->
    <select id="getPaymentById" resultType="payment">
        select * from payment where id=#{id}
    </select>



</mapper>